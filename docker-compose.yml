
services:

  front-end:
    build:
      context: ./front-end
      dockerfile: ./front-end.dockerfile
    restart: always
    ports:
      - "${FRONTEND_PORT}:80"
    deploy:
      mode: replicated
      replicas: 1